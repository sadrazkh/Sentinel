{
  "name": "sentinel-web",
  "private": true,
  "version": "1.0.0",
  "description": "Per-page Vue islands for the Sentinel portal. Not an SPA: ASP.NET Core MVC owns routing and rendering.",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.8",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "@fontsource-variable/vazirmatn": "^5.3.0",
    "vue": "^3.5.13"
  }
}
