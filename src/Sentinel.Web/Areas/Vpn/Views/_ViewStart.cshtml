@{
    @* The member-facing shell, not the admin one: this is a customer page that happens to live in
       its own area because the VPN module's policy is separate, not because its audience is. *@
    Layout = "_Layout";
}
