{
  "//": [
    "Reference for every setting the portal understands. Copy the values you need into",
    "environment variables or a secret store — never commit real secrets.",
    "Environment variables use double underscores for nesting:",
    "  Database__Provider=PostgreSql",
    "  ConnectionStrings__Sentinel=Host=db;Database=sentinel;Username=sentinel;Password=...",
    "  Seed__SuperAdmin__Password=..."
  ],

  "ConnectionStrings": {
    "//Sentinel": "Wins over Database:ConnectionString. Supply it as a secret, never in a file.",
    "Sentinel": "Host=localhost;Port=5432;Database=sentinel;Username=sentinel;Password=CHANGE_ME"
  },

  "Database": {
    "//Provider": "PostgreSql | Sqlite | SqlServer. Sqlite is for local development and tests only.",
    "Provider": "PostgreSql",
    "//MigrateOnStartup": "Fine for a single instance. With replicas, migrate as a deployment step.",
    "MigrateOnStartup": true,
    "CommandTimeoutSeconds": 30,
    "//EnableSensitiveDataLogging": "Logs query parameter values. Rejected outright in Production.",
    "EnableSensitiveDataLogging": false
  },

  "Security": {
    "//RequireHttps": "Forces HTTPS redirection, HSTS, Secure cookies and the __Host- cookie prefix.",
    "RequireHttps": true,
    "SessionLifetimeMinutes": 480,
    "SlidingExpiration": true,
    "//ForwardedHeaderHops": "Number of trusted reverse proxies. 0 disables X-Forwarded-* handling.",
    "ForwardedHeaderHops": 1,
    "Password": {
      "MinimumLength": 12,
      "RequiredUniqueChars": 5,
      "RequireDigit": true,
      "RequireLowercase": true,
      "RequireUppercase": false,
      "RequireNonAlphanumeric": false
    },
    "Lockout": {
      "MaxFailedAttempts": 5,
      "LockoutMinutes": 15
    },
    "LoginRateLimit": {
      "//": "Per source address, on top of Identity's per-account lockout.",
      "PermitLimit": 10,
      "WindowSeconds": 60,
      "QueueLimit": 0
    }
  },

  "SecurityHeaders": {
    "//ContentSecurityPolicyReportOnly": "Report violations without enforcing. Use while tightening a policy.",
    "ContentSecurityPolicyReportOnly": false,
    "ContentSecurityPolicyReportUri": null,
    "UpgradeInsecureRequests": true
  },

  "Membership": {
    "//GracePeriodDays": "Days of access after expiry so a late renewal does not lock a customer out.",
    "GracePeriodDays": 3,
    "RenewalWarningDays": 7
  },

  "Seed": {
    "SuperAdmin": {
      "//": "Bootstraps the first administrator. A no-op once any SuperAdmin exists.",
      "Enabled": false,
      "UserName": "admin",
      "Email": "admin@example.com",
      "DisplayName": "System Administrator",
      "//Password": "Supply through Seed__SuperAdmin__Password only. Clear it after first boot.",
      "Password": ""
    },
    "//IncludeSampleApplications": "Placeholder catalogue rows for local development. Rejected in Production.",
    "IncludeSampleApplications": false
  },

  "DataProtection": {
    "//KeyRingPath": "Mount a volume here so cookies survive a container redeploy. Empty = in-process keys.",
    "KeyRingPath": "/var/sentinel/keys"
  }
}
