<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- Only the Identity *model* types (IdentityUser/IdentityRole). No EF Core, no ASP.NET Core. -->
    <PackageReference Include="Microsoft.Extensions.Identity.Stores" />
  </ItemGroup>

</Project>
